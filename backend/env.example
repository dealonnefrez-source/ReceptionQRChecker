# Use PostgreSQL for online deployment, for example Neon or Supabase
RECEPTIONQR_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# Optional local SQLite fallback for development only
RECEPTIONQR_SQLITE_PATH=backend/data/reception_qr.sqlite3
